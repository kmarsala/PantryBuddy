<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

   <!-- <SwitchPreference
        android:defaultValue="true"
        android:key="example_switch"
        android:summary="@string/pref_pantry_notifications"
        android:title="@string/pref_title_pantry_notifications" />-->

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:defaultValue="1"
        android:entries="@array/pref_example_list_titles"
        android:entryValues="@array/pref_example_list_values"
        android:key="example_list"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_change_text_size" />

    <Preference
        android:key="example_text"
        android:summary="@string/pref_tutorial"
        android:title="@string/pref_title_tutorial" />
<!--
    <TextView
        android:id="@+id/text_view"
        android:text="Click this test"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="75sp"
        android:clickable="true"
        android:onClick="perform_action"
        />-->

<!--    <Preference
        android:key="tutorial"
        android:summary="@string/pref_tutorial"
        android:title="@string/pref_title_tutorial" />-->

</PreferenceScreen>
